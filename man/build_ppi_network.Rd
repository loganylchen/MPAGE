% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi_network.R
\name{build_ppi_network}
\alias{build_ppi_network}
\title{Build PPI Network (Revised)}
\usage{
build_ppi_network(
  data_sources = c("STRING", "BIOGRID", "INTACT"),
  filters = list(string = 700, intact = 0.7, biogrid = c("physical")),
  versions = list(string = "12", intact = "2025-03-28", biogrid = "4.4.248"),
  processed_dir = "./",
  species = "9606"
)
}
\arguments{
\item{data_sources}{Character vector of data sources to use ("STRING", "BIOGRID", "INTACT")}

\item{filters}{List of filtering criteria for each data source. Named list with elements
'string' (numeric score threshold, default: 700), 'intact' (numeric score threshold, default: 0.7),
'biogrid' (character vector of evidence types, default: c("physical"))}

\item{versions}{List of database versions to use. Named list with elements 'string', 'intact',
'biogrid' specifying version strings for each database}

\item{processed_dir}{Character string specifying directory path for processed data files (default: "./")}

\item{species}{Character string specifying species taxonomy ID (default: "9606")}
}
\value{
A named list of igraph objects, one per data source (e.g., \code{string}, \code{biogrid}, \code{intact}).
  Use \code{\link{merge_ppi_networks}} to combine them into a single network.
}
\description{
Construct protein-protein interaction network from multiple data sources using unified interface
}
\examples{
# Build network using RNA modification proteins
ppi_network <- build_ppi_network()

# Build network with specific proteins
rna_mod_proteins <- get_rna_mod_proteins()
ppi_network <- build_ppi_network()
}
