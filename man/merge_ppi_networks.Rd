% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi_network.R
\name{merge_ppi_networks}
\alias{merge_ppi_networks}
\title{Merge PPI Networks}
\usage{
merge_ppi_networks(networks, merge_method = "union", add_source_labels = TRUE)
}
\arguments{
\item{networks}{List of igraph objects to merge (typically from build_ppi_network())}

\item{merge_method}{Method to combine networks ("union" or "intersection"). "union" includes all interactions from any network, "intersection" includes only interactions present in ALL networks.}

\item{add_source_labels}{Whether to add source labels to edges indicating which databases contributed each interaction}
}
\value{
igraph object representing the merged network
}
\description{
Merge protein-protein interaction networks from multiple data sources
}
\examples{
\dontrun{
# Build individual networks
networks <- build_ppi_network(data_sources = c("STRING", "BIOGRID", "INTACT"))

# Merge all networks using union (all interactions)
merged_network <- merge_ppi_networks(networks, merge_method = "union")

# Merge using intersection (only interactions present in all networks)
intersected_network <- merge_ppi_networks(networks, merge_method = "intersection")

# Visualize merged network
plot(merged_network, vertex.size = 5, edge.width = 1)
}
}
