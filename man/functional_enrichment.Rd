% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R
\name{functional_enrichment}
\alias{functional_enrichment}
\title{Functional Enrichment Analysis}
\usage{
functional_enrichment(
  gene_set,
  key_proteins = NULL,
  organism = "hsa",
  p_value_cutoff = 0.05,
  q_value_cutoff = 0.2,
  min_genes = 5,
  max_genes = 500
)
}
\arguments{
\item{gene_set}{Vector of gene symbols for enrichment analysis}

\item{key_proteins}{Vector of key proteins to check for presence in enriched pathways}

\item{organism}{Organism identifier (default: "hsa" for human)}

\item{p_value_cutoff}{P-value cutoff for significance (default: 0.05)}

\item{q_value_cutoff}{Q-value/FDR cutoff for significance (default: 0.2)}

\item{min_genes}{Minimum number of genes required in a pathway (default: 5)}

\item{max_genes}{Maximum number of genes allowed in a pathway (default: 500)}
}
\value{
List containing enrichment results for KEGG, GO-BP, and WikiPathways
Each result includes pathway information, statistics, and key protein presence
}
\description{
Perform functional enrichment analysis using KEGG, GO-Biological Process, and WikiPathways
}
\examples{
\dontrun{
# Basic enrichment analysis
genes <- c("TP53", "BRCA1", "BRCA2", "ATM", "CHEK2")
results <- functional_enrichment(gene_set = genes)

# Enrichment with key proteins
key_prots <- c("TP53", "BRCA1")
results <- functional_enrichment(gene_set = genes, key_proteins = key_prots)

# Custom parameters
results <- functional_enrichment(
  gene_set = genes,
  key_proteins = key_prots,
  p_value_cutoff = 0.01,
  min_genes = 10
)
}
}
