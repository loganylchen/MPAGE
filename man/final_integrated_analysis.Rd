% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_pairs.R
\name{final_integrated_analysis}
\alias{final_integrated_analysis}
\title{Final Integrated Analysis}
\usage{
final_integrated_analysis(
  expression_matrix,
  sample_condition,
  modules,
  module_pairs = NULL,
  min_samples = 3,
  activity_method = c("gsva", "ssgsea", "zscore", "plage"),
  temp_dir = "./temp"
)
}
\arguments{
\item{expression_matrix}{Gene expression matrix (genes as rows, samples as columns)}

\item{sample_condition}{Named vector indicating sample conditions ("case" or "control")}

\item{modules}{List of modules, each containing genes (e.g., list(module1 = c("gene1", "gene2"), module2 = c("gene3", "gene4")))}

\item{module_pairs}{List of specific module pairs to compare. If NULL, compares all possible pairs.}

\item{min_samples}{Minimum samples per condition for valid analysis (default: 3)}

\item{activity_method}{List of methods to calculate module activity. Default: c("gsva", "ssgsea", "zscore", "plage")}
}
\value{
List containing results from all three analysis types with metadata
}
\description{
Automatically run all types of gene pair analyses (intra-module, inter-module,
and module activity comparison) and return comprehensive results.
}
\examples{
\dontrun{
# Create example data
expr_mat <- matrix(rnorm(1000), nrow = 100, ncol = 10)
colnames(expr_mat) <- paste0("sample_", 1:10)
rownames(expr_mat) <- paste0("gene_", 1:100)

# Define conditions
conditions <- c(
  sample_1 = "case", sample_2 = "case", sample_3 = "case",
  sample_4 = "case", sample_5 = "case",
  sample_6 = "control", sample_7 = "control", sample_8 = "control",
  sample_9 = "control", sample_10 = "control"
)

# Define modules
modules <- list(
  module1 = c("gene_1", "gene_2", "gene_3"),
  module2 = c("gene_4", "gene_5", "gene_6"),
  module3 = c("gene_7", "gene_8", "gene_9")
)

# Run integrated analysis
final_results <- final_integrated_analysis(expr_mat, conditions, modules)

# View results
print(final_results$summary)
head(final_results$intra_module_results)
head(final_results$inter_module_results)
head(final_results$module_activity_results)
}
}
