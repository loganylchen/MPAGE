% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_pairs.R
\name{module_activity_comparison}
\alias{module_activity_comparison}
\title{Module Activity Comparison}
\usage{
module_activity_comparison(
  expression_matrix,
  sample_condition,
  modules,
  module_pairs = NULL,
  min_samples = 3,
  activity_method = "gsva",
  temp_dir = "./temp"
)
}
\arguments{
\item{expression_matrix}{Gene expression matrix (genes as rows, samples as columns)}

\item{sample_condition}{Named vector indicating sample conditions ("case" or "control")}

\item{modules}{List of modules, each containing genes (e.g., list(module1 = c("gene1", "gene2"), module2 = c("gene3", "gene4")))}

\item{module_pairs}{List of module pairs to compare. If NULL, compares all possible pairs.}

\item{min_samples}{Minimum samples per condition for valid analysis (default: 3)}

\item{activity_method}{Method to calculate module activity (default: "gsva", options: "gsva", "ssgsea", "zscore", "plage")}
}
\value{
Data frame with module activity comparison results including Fisher test statistics
}
\description{
Compare module activity scores between case and control conditions using Fisher exact test.
First calculates module activity for each sample using GSVA or specified method,
then tests if module1 activity is significantly greater than module2 activity.
}
\examples{
\dontrun{
# Create sample expression data
expr_mat <- matrix(rnorm(1000), nrow = 100, ncol = 10)
colnames(expr_mat) <- paste0("sample_", 1:10)
rownames(expr_mat) <- paste0("gene_", 1:100)

# Define conditions
conditions <- c(
  sample_1 = "case", sample_2 = "case", sample_3 = "case",
  sample_4 = "case", sample_5 = "case",
  sample_6 = "control", sample_7 = "control", sample_8 = "control",
  sample_9 = "control", sample_10 = "control"
)

# Define modules
test_modules <- list(
  module1 = list(module_id = "module1", genes = paste0("gene_", 1:10)),
  module2 = list(module_id = "module2", genes = paste0("gene_", 11:20))
)

# Compare module activity
results <- module_activity_comparison(
  expression_matrix = expr_mat,
  sample_condition = conditions,
  modules = test_modules,
  activity_method = "zscore"
)
}

}
